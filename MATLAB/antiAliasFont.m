function newSignalMatrix = antiAliasFont(signalMatrix, scale_factor)

    [h,w,nLetters] = size(signalMatrix);
    
    h_new = h/scale_factor;
    w_new = w/scale_factor;
    
    for i = 1:nLetters
        newSignalMatrix = 
        
        
    end
        
        





end